<Page x:Class="lab4_5.Pages.SuccessfulPurchase"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:lab4_5.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SuccessfulPurchase">

    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#fff"/>
        </Style>
    </Page.Resources>

    <Grid Margin="0 100">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*" />
            <RowDefinition Height="100*" />
            <RowDefinition Height="40*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Билет успешно куплен" FontSize="44" HorizontalAlignment="Center" Foreground="#fff"/>
        <Image Grid.Row="1" Source="/Resources/img/successful.png" />
        <Button Grid.Row="2" Content="Вернуться на главную" FontSize="28" HorizontalAlignment="Center" Foreground="#fff" Background="Transparent" FlowDirection="RightToLeft" Click="Button_Click"/>
    </Grid>
    
    
</Page>
